基于参考现有Anti-DETR四大核心创新（小波残差网络GWRN、自适应全局校准注意力AGCAU、直方图尺度交互特征HIFI、端到端检测框架），当前模型已解决空对空无人机检测的小目标特征缺失、多尺度融合弱、背景抗干扰差等基础问题。结合空对空场景极端检测难点、DETR系列模型现存瓶颈及前沿研究趋势，可从模块精细化升级、复杂极端场景适配、架构轻量化革新、多模态融合、时序建模、损失函数优化、落地工程化七大维度开展深度创新，所有方向均与现有工作无缝衔接，无重复创新，可显著提升模型性能与实用性。

一、模块精细化升级：解决现有核心模块固有短板

1. 动态自适应小波残差网络（升级原有GWRN）  
    现有GWRN采用固定小波卷积与全局多通道注意力，对空域多变、远近尺度剧变的无人机目标适配性有限，且固定小波参数无法适配雾霾、强光、低空杂波等差异化空域背景。可创新设计动态频率自适应小波卷积，根据输入图像的空域高频、低频特征分布，实时调整小波分解层级与滤波系数：针对远距离极小无人机强化高频细节提取，保留目标边缘特征；针对近距离大尺寸无人机侧重低频语义特征提取，避免特征冗余。同时，将固定全局注意力改造为稀疏全局多通道注意力，屏蔽天空云层、飞鸟、光斑等无效背景区域的注意力权重，解决原全局注意力易被复杂空域背景干扰的问题，进一步提升小目标特征提纯能力。
2. 双向递归多尺度精炼特征金字塔（升级原有MRFPN-AGCAU）  
    现有AGCAU仅实现高低分辨率特征的单向融合与权重调整，存在浅层细节丢失、深层语义偏移的问题，对高速运动、姿态畸变的无人机位置校准精度不足。可创新双向递归校准AGCAU机制，构建浅层细节向深层传播、深层语义向浅层反馈的双向融合链路，实现多尺度特征循环迭代精炼。同时引入可变形尺度权重自适应机制，针对无人机倾斜、旋转、模糊等空域畸变特征，动态修正特征对齐偏差，解决传统金字塔网络小目标位置偏移、边缘特征模糊的痛点，大幅提升微小无人机、畸变无人机的定位精度。
3. 多维度像素感知HIFI特征增强（升级原有HIFI）  
    现有HIFI仅基于像素强度分割特征、实现尺度交互融合，未考虑空域图像的纹理、梯度、噪声分布特征，对低对比度、隐身涂层无人机识别能力薄弱。可升级为多特征联合分割的尺度交互机制，融合像素强度、边缘梯度、纹理特征三维信息动态分割空间特征，替代单一像素强度分割。同时新增跨尺度噪声抑制门控单元，区分无人机目标弱特征与天空噪声、云层纹理干扰，针对性增强目标特征、压制背景伪特征，彻底解决复杂空对空场景下小目标特征淹没、误检率偏高的问题。


|  实验组别   |            模块配置             |
| :---------: | :-----------------------------: |
|   Exp1(A)   |   动态GWRN+单向AGCAU+原始HIFI   |
|  Exp2(A+B)  |   动态GWRN+双向AGCAU+原始HIFI   |
| Exp3(A+B+C) | 动态GWRN+双向AGCAU+多维降噪HIFI |
